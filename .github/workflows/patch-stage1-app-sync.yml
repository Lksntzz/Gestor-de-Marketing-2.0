name: Patch Stage 1 App Sync

on:
  push:
    branches:
      - feat/3.1-smart-knowledge-stage1
    paths:
      - scripts/patch-stage1-app-sync.py
      - .github/workflows/patch-stage1-app-sync.yml

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/3.1-smart-knowledge-stage1
          fetch-depth: 0
      - name: Patch manual sync
        run: python3 scripts/patch-stage1-app-sync.py
      - name: Remove temporary patch machinery
        run: |
          rm -f scripts/patch-stage1-app-sync.py
          rm -f .github/workflows/patch-stage1-app-sync.yml
      - name: Commit patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: make manual Obsidian sync REST-first"
          git push origin HEAD:feat/3.1-smart-knowledge-stage1
